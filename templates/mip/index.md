<!--
File: docs/engineering/protocols/mip-nnn-subject-protocol/index.md
Document: MIP-NNN
Status: Draft
-->

<!--
Guidance
- A MIP defines a machine-readable contract between independently developed components. It is
  Diataxis reference material.
- A MIP is the only document type that defines a versioned artefact. Declare the contract version
  here, on the landing page, so a reader knows immediately what they are implementing.
- The version belongs to the contract, not to this Markdown. Major integer only, no minor or patch.
  The document itself carries a Status like every other document. See MDG-001 chapter 03.
- Keep implementation-specific behaviour out. A MIP says what must be true on the wire, not how any
  particular component achieves it.
-->

# MIP-NNN — Subject Protocol

> *A single sentence capturing what this protocol keeps stable.*

---

# Contract Version

This document defines **Subject Protocol v1**.

<!--
A new major version is issued only when a change would break an existing conforming implementation.
Backward-compatible additions do not produce a new version; record them in the revision history.
A MIP may describe more than one contract version while both remain supported.
-->

---

# Purpose

MIP-NNN defines the contract used by ...

The engineering practice that builds against this contract is explained by [ID — Canonical Title](relative/path/index.md). This protocol defines what those implementations must preserve.

---

# Protocol Statement

Within Mosaic:

> **The statement this protocol exists to establish.**

---

# Scope

This protocol defines:

- the contract surface it governs

It does not define:

- implementation internals, with a link to the document that covers them
